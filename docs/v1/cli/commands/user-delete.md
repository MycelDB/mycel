# `mycel user delete`

Deletes a user.

## Example

```sh
mycel -d ./data -u admin -p change-me user delete <user_id>
```

## Notes

Deleting a user is a hard delete and also deletes spaces owned by that user and associated constructs.
