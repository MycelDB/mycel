# `mycel acl revoke`

Revokes a system role or space permission.

## Examples

```sh
mycel -d ./data -u admin -p change-me acl revoke system --user-id <user_id>
mycel -d ./data -u admin -p change-me acl revoke space --space-id <space_id> --user-id <user_id>
```
