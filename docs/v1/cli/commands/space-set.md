# `mycel space set`

Sets the current working space in the REPL session.

## Example

```text
mycel> space set <space_id>
```

After setting a space, space-specific commands may omit `--space-id` when supported.
