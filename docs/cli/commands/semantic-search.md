# `mycel semantic search`

Target advanced command that searches one or more semantic indexes.

## Example

```sh
mycel semantic search --space-id <space_id> --domain personal-pkm --index notes-search --text "sleep, exercise, and focus"
```

## Notes

Semantic search plans over semantic indexes and vector spaces. Query embeddings may use any compatible explicit grant.
