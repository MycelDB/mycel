# `mycel accounting usage rebuild-indexes`

Rebuilds derived accounting indexes from the authoritative usage ledger.

## Example

```sh
mycel accounting usage rebuild-indexes
```

## Notes

Indexes are derived from `meta/accounting/inference-usage-*.kusage` and can be rebuilt if missing or corrupt.
