package cmd

import "testing"

func TestSemanticIndexBackfillCLI(t *testing.T) {
	t.Skip("embedded semantic backfill CLI removed in daemon-only phase 2; daemon coverage lives in semantic daemon tests")
}
